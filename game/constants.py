NUM_PLAYERS = 2

# If numerical stability epsilon is used broadly across game logic (not just CFR),
# keep it here. Otherwise, the CFR-specific epsilon will live in models/cfr/constants.py
# EPSILON = 1e-10
